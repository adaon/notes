elem.setPreferredSize(new Dimension(int w, int h)); // Установка размера элемента
elem.setMinumumSize(new Dimension(int w, int h)); // Установка минимального размера элемента
elem.setMaximunSize(new Dimension(int w, int h)); // Установка максимального размера элемента