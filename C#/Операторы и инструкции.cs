int x = x > 0 ? 1 : 2; // Тернарный оператор

// Цикл foreach
foreach(int value in array) {
    // ...
}

x && y // Логическое И

if(myClass is MyClass) // Является ли объект экземпляром определенного класса